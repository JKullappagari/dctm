{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "MasterData": "Server=localhost,1433;Database=dctrack;User ID=masterdata_svc;Password=CHANGE_ME;TrustServerCertificate=True"
  },
  "Cors": {
    "Origins": [
      "http://localhost:4200", "http://localhost:4201", "http://localhost:4202",
      "http://localhost:8090", "http://localhost:8091", "http://localhost:8092"
    ]
  },
  "Authorization": {
    "// Enforce": "When true, mutating verbs require the legacy (module, right) via JWT. Off in dev so the tokenless standalone MFE (:4201) and tests keep working; the shell + prod set it true.",
    "Enforce": false
  }
}
